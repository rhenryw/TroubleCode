This is a little GZIP code for transmitting info over email/IM. So I can view the user's deployment issues without 800 screenshots.

The JS that generates the code is mine but the UI for displaying it is AI because I am the only person that will use this